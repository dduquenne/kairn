/**
 * Image utilities — re-exports from @kairn/core/storage
 *
 * Provides blur placeholders and image dimension constants
 * to prevent CLS (Cumulative Layout Shift).
 */

export {
  BLUR_DATA_URL,
  BLUR_DATA_URL_GOLD,
  IMAGE_DIMENSIONS,
  ASPECT_RATIOS,
  getPlaceholderProps,
  getImageProps,
} from '@kairn/core/storage/image-utils';
