name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: ${{ vars.TURBO_TEAM }}

jobs:
  # Lint and type-check run in parallel
  lint:
    name: Lint (${{ matrix.app }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        app: [psypnos, avv]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm --filter @kairn/db db:generate

      - name: Lint
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            pnpm turbo run lint --filter='@kairn/${{ matrix.app }}...[origin/main]'
          else
            pnpm turbo run lint --filter='@kairn/${{ matrix.app }}...'
          fi

  type-check:
    name: Type Check (${{ matrix.app }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        app: [psypnos, avv]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm --filter @kairn/db db:generate

      - name: Type check
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            pnpm turbo run type-check --filter='@kairn/${{ matrix.app }}...[origin/main]'
          else
            pnpm turbo run type-check --filter='@kairn/${{ matrix.app }}...'
          fi

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm --filter @kairn/db db:generate

      - name: Run unit tests with coverage
        run: pnpm test:coverage

      - name: Run UI component tests
        run: pnpm test:ui

      - name: Run accessibility tests
        run: pnpm test:a11y

      - name: Check coverage threshold
        run: |
          # Extract coverage percentage from coverage summary
          COVERAGE=$(cat coverage/coverage-summary.json 2>/dev/null | jq -r '.total.lines.pct // 0' || echo "0")
          echo "Current coverage: ${COVERAGE}%"
          # Fail if coverage is below 60%
          if (( $(echo "$COVERAGE < 60" | bc -l) )); then
            echo "::error::Coverage is below 60% threshold (current: ${COVERAGE}%)"
            exit 1
          fi
          echo "::notice::Coverage threshold met: ${COVERAGE}% >= 60%"

      - name: Upload coverage reports
        uses: codecov/codecov-action@v4
        if: always()
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/lcov.info
          fail_ci_if_error: false

  e2e:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: [build]
    # E2E requires a running dev server with database access
    # Set E2E_ENABLED=true in repository variables to activate
    if: vars.E2E_ENABLED == 'true'
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      JWT_ACCESS_SECRET: ${{ secrets.JWT_ACCESS_SECRET }}
      JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        run: pnpm --filter @kairn/psypnos exec playwright install --with-deps chromium

      - name: Run E2E tests
        run: pnpm --filter @kairn/psypnos test:e2e --project=chromium
        env:
          CI: true

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: apps/psypnos/playwright-report/
          retention-days: 7

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Audit dependencies
        # Using critical level because:
        # - xlsx has known high vulnerabilities with no patch available (Patched versions: <0.0.0)
        # - Next.js high vulnerabilities require migration to v15+ (breaking changes)
        # These are tracked and will be addressed when patches become available
        run: pnpm audit --audit-level=critical

      - name: Check for secrets
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event_name == 'pull_request' && github.event.repository.default_branch || github.event.before }}
          head: HEAD
          extra_args: --only-verified

  # Build runs after all checks pass — each app builds independently
  build:
    name: Build (${{ matrix.app }})
    runs-on: ubuntu-latest
    needs: [lint, type-check, test, security]
    strategy:
      fail-fast: false
      matrix:
        app: [psypnos, avv]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm --filter @kairn/db db:generate

      - name: Build
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            pnpm turbo run build --filter='@kairn/${{ matrix.app }}...[origin/main]' --env-mode=loose
          else
            pnpm turbo run build --filter='@kairn/${{ matrix.app }}...' --env-mode=loose
          fi
